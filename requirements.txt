# Python dependencies for local tooling (data inspection / preparation).
# The website itself has NO build step and needs none of these to run — it is
# plain static files that load their JS libraries from a CDN.
#
# Set up a local environment with:
#   python3 -m venv .venv
#   . .venv/bin/activate
#   pip install -r requirements.txt

polars>=1.0
